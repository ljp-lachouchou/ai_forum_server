# 智汇社区 (AI-Community) 项目规划文档

## 第一部分：产品需求文档 (PRD)

**项目名称：** 智汇社区 (AI-Community) **项目愿景：** 打造一个融合知识获取效率与情感宣泄空间的智能化社区。

### 1. 用户角色与目标

| 角色                         | 目标/需求                                      |
| ---------------------------- | ---------------------------------------------- |
| **普通用户** (Seeker/Sharer) | 快速获取游戏攻略；分享和倾诉情绪；与社区互动。 |
| **内容创作者** (Contributor) | 高效发布优质内容；获得关注和成就感。           |
| **管理员** (Admin)           | 高效维护社区秩序，处理违规内容。               |

### 2. 功能模块详细说明

#### 模块 A：基础论坛 (The Foundation)

| ID   | 功能点     | 描述                                                         |
| ---- | ---------- | ------------------------------------------------------------ |
| F01  | 用户认证   | 支持手机号、第三方登录（微信、Apple ID）。                   |
| F02  | 首页信息流 | 支持 **“关注”** (Follow)、**“推荐”** (基于热度)、**“最新”** 三种模式切换。 |
| F03  | 社区结构   | 主板区划分（如：游戏专区、闲聊区）；支持话题标签 (#Hashtags)。 |
| F04  | 内容发布   | 支持富文本（文本、图片、链接），支持 Markdown 格式（可选）。 |
| F05  | 互动系统   | 点赞、评论（盖楼模式）、收藏、转发、通知中心。               |

#### 模块 B：AI 智能助手 (AI Integration)

| ID   | 功能点           | 描述                                                         | 核心技术                 |
| ---- | ---------------- | ------------------------------------------------------------ | ------------------------ |
| F06  | **AI 攻略总结**  | 用户搜索时，系统检索站内相关帖子，AI 自动生成一段带引用来源的精准总结。 | **RAG (检索增强生成)**   |
| F07  | **辅助发帖**     | 用户输入正文后，AI 自动生成 **3 个吸睛标题**和 **5 个相关标签**供用户选择。 | LLM (Prompt Engineering) |
| F08  | **评论区总结**   | 针对热门帖子（评论 > 50条），AI 自动总结评论区的**核心观点**和**舆论倾向**。 | LLM (观点聚类)           |
| F09  | **智能内容审核** | 帖子发布前，AI 自动审核文本，识别**辱骂、色情、广告**等违规内容并拦截。 | LLM (Moderation API)     |

#### 模块 C：树洞空间 (The Tree Hole)

| ID   | 功能点          | 描述                                                         |
| ---- | --------------- | ------------------------------------------------------------ |
| F10  | 独立入口/UI     | 独立的页面入口，采用柔和、私密的 UI 主题，与主论坛区分。     |
| F11  | **强制匿名**    | 发布时自动分配随机昵称（如“忧郁的蘑菇”），不关联主账号。     |
| F12  | **情绪记录**    | 用户选择当前心情（开心/难过/焦虑），UI 主题随心情动态切换。  |
| F13  | **AI 暖心回响** | 若树洞帖子发布 30 分钟内无人回复，AI 自动生成一条共情、温暖的评论。 |
| F14  | 隐私设置        | 支持**“仅自己可见”**（个人日记模式）和**“阅后即焚”**（24小时自动删除）。 |

## 第二部分：技术设计文档 (TDD)

**核心设计理念：** Kotlin Multiplatform 驱动客户端，Python 生态赋能 AI 后端，使用 PostgreSQL 实现统一的数据和向量存储。

### 1. 技术栈选型 (Tech Stack)

| 模块                   | 推荐技术                         | 理由                                                         |
| ---------------------- | -------------------------------- | ------------------------------------------------------------ |
| **客户端 (App)**       | **Kotlin Compose Multiplatform** | 一套代码可运行在 Android、iOS 甚至 Desktop；**Compose** 状态驱动 UI 非常适合处理动态主题（树洞）和 AI 内容。 |
| **后端服务 (Backend)** | **Python (FastAPI)**             | Python 是 AI 生态的原生语言；**FastAPI** 性能高，异步支持好，与 AI 库（如 LangChain）完美集成。 |
| **数据库**             | **PostgreSQL (with pgvector)**   | 统一存储关系型数据和**向量数据** (Embedding)，无需引入额外的向量数据库，降低架构复杂度。 |
| **AI/LLM 服务**        | **DeepSeek/Qwen API**            | 优秀的中文能力，API 成本低，响应速度快。                     |
| **网络/DI**            | **Ktor Client (KMP)** / **Koin** | KMP 生态标准库，完美支持协程和依赖注入。                     |

### 2. 核心系统架构图

此架构图展示了数据、业务逻辑和 AI 服务之间的流转关系。

graph TD
    User[用户 APP (Compose CMP)] -->|HTTP/REST| API[FastAPI 后端网关]
    

    subgraph "后端服务 (Python/FastAPI)"
        API --> Auth[认证模块]
        API --> Forum[论坛业务模块]
        API --> Hole[树洞业务模块]
        API --> Search[AI 搜索模块]
    end
    
    subgraph "数据存储 (PostgreSQL)"
        Forum --> PG[(PostgreSQL)]
        Hole --> PG
        Search --> PG_Vector[(PG Vector 插件)]
    end
    
    subgraph "AI 服务 (LLM APIs)"
        Search -->|调用| LLM_API[大模型 API (总结/回答)]
        Hole -->|调用| LLM_API
        Post_Process(帖子发布) -->|调用| Embed_API[Embedding API (向量化)]
    end

### 3. 关键功能实现逻辑：AI RAG 流程

实现 **F06 (AI 攻略总结)** 的核心是 **检索增强生成 (RAG)**，该流程由后端 (FastAPI) 管理：

| 步骤                     | 模块/组件           | 描述                                                         | 关键技术                               |
| ------------------------ | ------------------- | ------------------------------------------------------------ | -------------------------------------- |
| **1. 录入 (Ingestion)**  | 帖子服务 / 异步任务 | 用户发布攻略 -> 调用 Embedding API -> 获得 1536 维的向量 -> 存入 `post_embeddings` 表。 | Embedding 模型 / PostgreSQL `pgvector` |
| **2. 提问 (Query)**      | AI 搜索服务         | 用户输入搜索词 (如：“呀哈哈位置”) -> 将问题发送给 Embedding API -> 获得问题的向量。 | Embedding 模型                         |
| **3. 检索 (Retrieval)**  | AI 搜索服务 / DB    | 使用问题向量在 `pgvector` 表中进行**向量相似度查询**（例如：余弦相似度 cos(θ)），召回 Top N (例如 N=3) 最相似的攻略原文。 | cos(θ) 相似度计算                      |
| **4. 生成 (Generation)** | AI 搜索服务 / LLM   | 将召回的 Top N 原文和用户问题一起组成 **Prompt** 发送给 LLM API，要求其进行总结。 | LLM (DeepSeek/Qwen)                    |
| **5. 交付 (Delivery)**   | API 网关 / Compose  | LLM 返回总结结果和引用列表 -> FastAPI 封装为 JSON -> Compose App 渲染。 | Ktor Client / Compose UI               |

### 4. 客户端 (Compose Multiplatform) 关键设计

#### 4.1 数据流架构

采用 **单向数据流 (UDF)** 模式：

- **View (Compose UI)** 发送 **Event** (用户点击、输入)。
- **ViewModel** 接收 Event，执行业务逻辑（调用 Ktor 网络服务）。
- **ViewModel** 通过 **StateFlow** 或 **State** 暴露 **State** (UI 状态)。
- **View** 收集 State，并根据 State 变化重绘 UI。

#### 4.2 树洞动态主题实现 (F12)

```kotlin
// 核心 Compose 状态代码片段 (伪代码)
@Composable
fun TreeHoleScreen(viewModel: TreeHoleViewModel) {
    // 1. 监听 ViewModel 暴露的心情状态
    val currentMood by viewModel.currentMood.collectAsState() 
    
    // 2. 状态驱动，根据心情平滑过渡背景颜色
    val backgroundColor by animateColorAsState(
        targetValue = when(currentMood) {
            Mood.Happy -> Color(0xFFFFE082) 
            Mood.Sad -> Color(0xFF90A4AE)   
            else -> Color.White 
        }
    )

    // 3. UI 容器使用动态背景
    Box(modifier = Modifier.background(backgroundColor).fillMaxSize()) {
        // ... 内容布局
        MoodSelector(onMoodSelected = viewModel::setMood) 
    }
}  
```

### 5. API 接口设计 (FastAPI)

| 方法     | 路径                                 | 描述                  | 核心操作                                                   |
| -------- | ------------------------------------ | --------------------- | ---------------------------------------------------------- |
| **POST** | `/api/v1/user/login`                 | 用户登录/注册         | JWT 生成                                                   |
| **POST** | `/api/v1/posts`                      | 发布普通帖子          | 数据库写入 / **触发 F07 辅助发帖** / **触发 F09 智能审核** |
| **POST** | `/api/v1/search/ai`                  | **AI 智能搜索 (F06)** | 触发 RAG 流程                                              |
| **POST** | `/api/v1/treehole`                   | 发布树洞 (F11/F12)    | 数据库写入 / **触发 F13 AI 暖心回响的 30min 定时任务**     |
| **GET**  | `/api/v1/post/{id}/comments/summary` | **评论区总结 (F08)**  | 获取评论 / LLM 总结                                        |

### 总结与下一步行动

这份 PRD 和 TDD 已经包含了您项目的所有核心功能和技术实现路径。

您选择了 **Compose Multiplatform** 和 **Python FastAPI**，这是一种面向未来的、高效率的组合。

**我建议的下一步是：**

我们从最容易上手的 **F07 (辅助发帖)** 开始，因为它只需要一个简单的 FastAPI 接口调用 LLM API 即可实现。









| **模块**     | **功能点 (ID)** | **需求接口**                     | **状态**   | **备注**                        |
| ------------ | --------------- | -------------------------------- | ---------- | ------------------------------- |
| **Auth**     | F01             | `POST /api/v1/user/login`        | **已完成** | 对应截图中的“登录”              |
|              | F01             | `POST /api/v1/user/register`     | **已完成** | 对应截图中的“注册”              |
| **Profile**  | -               | `GET /api/v1/user/me`            | **已完成** | 对应截图中的“获取个人信息”      |
|              | -               | `PUT /api/v1/user/me`            | **已完成** | 对应截图中的“更新个人信息”      |
| **Persona**  | -               | `PUT /api/v1/persona`            | **已完成** | 对应截图中的“更新用户画像”      |
| **AI 搜索**  | F06             | `POST /api/v1/search/ai`         | **已完成** | 对应截图中的“搜索-ai总结”       |
| **论坛业务** | F02/F03         | `GET /api/v1/posts`              | **未完成** | 首页信息流（关注/推荐/最新）    |
|              | F04             | `POST /api/v1/posts`             | **未完成** | 发布普通帖子                    |
|              | F05             | `POST /api/v1/posts/{id}/like`   | **未完成** | 点赞/收藏/评论互动系统          |
|              | F07             | `POST /api/v1/ai/post-assist`    | **未完成** | **AI 辅助发帖** (生成标题/标签) |
|              | F08             | `GET /api/v1/posts/{id}/summary` | **未完成** | **AI 评论区总结**               |
| **树洞空间** | F11/F12         | `POST /api/v1/treehole`          | **未完成** | 发布匿名树洞（带心情状态）      |
|              | F14             | `GET /api/v1/treehole/stream`    | **未完成** | 树洞流（匿名显示）              |

| **方法**   | **路径**                              | **描述**              | **关键逻辑**                                              |
| ---------- | ------------------------------------- | --------------------- | --------------------------------------------------------- |
| **POST**   | `/api/v1/posts/{post_id}/comments`    | **发布评论**          | 向 `comments` 表写入数据，`author_id` 从 JWT Token 获取。 |
| **GET**    | `/api/v1/posts/{post_id}/comments`    | **获取评论列表**      | 联表查询 `users` 表以获取评论者的头像和昵称。             |
| **DELETE** | `/api/v1/comments/{comment_id}`       | **删除评论**          | 需校验 `author_id` 是否为当前操作用户。                   |
| **GET**    | `/api/v1/posts/{id}/comments/summary` | **AI 评论总结 (F08)** | 获取该帖子的所有评论，发送给 LLM 生成核心观点。           |

**提示：** 你的表结构目前没有 `parent_id`。如果需要实现“盖楼模式”（回复某人的评论），建议在 `comments` 表中增加一个 `parent_id` 字段指向 `id`。

| **方法** | **路径**                          | **描述**             | **实现思路**                                                 |
| -------- | --------------------------------- | -------------------- | ------------------------------------------------------------ |
| **POST** | `/api/v1/posts/{post_id}/like`    | **点赞/取消点赞**    | 检查 `post_likes` 表是否存在记录，有则删（取消），无则加（点赞）。 |
| **POST** | `/api/v1/posts/{post_id}/collect` | **收藏/取消收藏**    | 同上，操作 `post_collections` 表。                           |
| **GET**  | `/api/v1/user/likes`              | **获取我点赞的帖子** | 用于个人中心的“赞过”列表。                                   |
